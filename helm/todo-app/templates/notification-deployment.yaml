apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.notification.name }}
  labels:
    app: {{ .Values.notification.name }}
    version: {{ .Values.image.tag | default .Chart.AppVersion }}
spec:
  replicas: {{ .Values.notification.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Values.notification.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.notification.name }}
        version: {{ .Values.image.tag | default .Chart.AppVersion }}
      annotations:
        {{- if .Values.dapr.enabled }}
        dapr.io/enabled: "true"
        dapr.io/app-id: {{ .Values.notification.dapr.appId }}
        dapr.io/enable-api-logging: "true"
        {{- end }}
    spec:
      {{- if .Values.image.pullSecrets }}
      imagePullSecrets:
        {{- toYaml .Values.image.pullSecrets | nindent 8 }}
      {{- end }}
      containers:
      - name: notification-service
        image: "{{ .Values.image.registry }}/{{ .Values.notification.image.repository }}:{{ .Values.notification.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: postgres-credentials
              key: connection-string
        - name: KAFKA_BOOTSTRAP_SERVERS
          value: "{{ .Release.Name }}-kafka:9092"
        - name: LOG_LEVEL
          value: INFO
        resources:
          {{- toYaml .Values.notification.resources | nindent 10 }}
        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
